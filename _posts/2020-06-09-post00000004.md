---
layout: post
title: My Vim Setting
category: others
---

## 개요

> 내가 사용하는 vimrc 설정에 대해 기록한다.
> * 자세한 설명은 이후 추가 예정

## vimrc

``` bash
"return" 2>&- || "exit"

" ===== Default Settings =====
syntax on
packadd! dracula
colorscheme dracula
"highlight ColorColumn ctermbg=Green
hi Search ctermbg=LightYellow       " 하이라이트 컬러(foreground color)
hi Search ctermfg=Red               " 하이라이트 컬러(background color)

set encoding=utf-8
set fileencodings=utf-8,cp949
set fileencoding=iso-8859-1

set number                          " 라인넘버 표시
set hlsearch                        " 검색 결과 강조
set tabstop=4                       " Tab 사이즈 4칸
set autoindent                      " 자동 들여쓰기
set shiftwidth=4                    " 자동 들여쓰기 SPACE 4칸으로설정
set expandtab                       " Tab 문자를 공백으로 변환
set cursorline                      " 현재 마우스 커서 라인 표시
set cursorcolumn                    " 커서 라인 표시
set list lcs=tab:\|.,trail:.        " 공백 표시
set backspace=indent,eol,start      " indent 표시 (.)
set incsearch                       " 키워드 검색 시 점진적 검색
set wildmenu                        " 자동완성 기능 향상
set splitright
set mouse=a
set ttymouse=xterm2

set omnifunc=ccomplete#Complete
set omnifunc=pythoncomplete#Complete
set omnifunc=python3complete#Complete

augroup python
    au!
    autocmd FileType python set colorcolumn=80
augroup END

"set mouse=a                         " 마우스 스크롤 허용
"set hlsearch                        " 하이라이트 적용
"set nowrapscan                      " 검색 시 처음으로 돌아가는 것을 막는다


" ==== highlight split pane ====
" Dim inactive windows using 'colorcolumn' setting
" This tends to slow down redrawing, but is very useful.
" Based on https://groups.google.com/d/msg/vim_use/IJU-Vk-QLJE/xz4hjPjCRBUJ
" XXX: this will only work with lines containing text (i.e. not '~')
function! s:DimInactiveWindows()
  for i in range(1, tabpagewinnr(tabpagenr(), '$'))
    let l:range = ""
    if i != winnr()
      if &wrap
        " HACK: when wrapping lines is enabled, we use the maximum number
        " of columns getting highlighted. This might get calculated by
        " looking for the longest visible line and using a multiple of
        " winwidth().
        let l:width=256 " max
      else
        let l:width=winwidth(i)
      endif
      let l:range = join(range(1, l:width), ',')
    endif
    call setwinvar(i, '&colorcolumn', l:range)
  endfor
endfunction
augroup DimInactiveWindows
  au!
  au WinEnter * call s:DimInactiveWindows()
  au WinEnter * set cursorline
  au WinLeave * set nocursorline
augroup END


" ==== Key Maaping ===
" 마수으스 스크롤 허용 F1 (on), F2 (off)
map <F1> <ESC>:set mouse=a<CR>
map <F2> <ESC>:set mouse-=a<CR>
" 커서 앞뒤로 이동 (Ctrl + h, or Ctrl + l)
map <C-h> <Home>
map <C-l> <End>


" ==== Ctags Setting ====
set tags=~/.tags


" ===== Plugin Import =====
set rtp+=~/.vim/bundle/Vundle.vim
call vundle#begin()

" let Vundle manage Vundle
" required!
Plugin 'VundleVim/Vundle.vim'

" My Bundles here:
Plugin 'dracula/vim', { 'name': 'dracula' }
Plugin 'vim-scripts/vcscommand.vim'       " git 소스 버전 컨트롤
Plugin 'Raimondi/delimitMate'             " 괄호 자동 완성 플러그인
Plugin 'airblade/vim-gitgutter'           " 추가,삭제,변경내역
Plugin 'tpope/vim-fugitive'               " 상태바에 깃 정보 플러그인 (airline와 연계)
Plugin 'gregsexton/gitv'                  " git 로그 뷰어 (:gitv 로 사용)
Plugin 'vim-airline/vim-airline'          " 상태바 정보 생성 플러그인
Plugin 'ctrlpvim/ctrlp.vim'               " 파일 검색 하는 플로그인 (ctrl + p)
Plugin 'scrooloose/nerdtree'              " 파일 탐색기 플러그인 (nerd 입력시 토글)
Plugin 'bad-whitespace'                   " 문자열 마지막 부분 공백 삭제
Plugin 'Yggdroot/indentLine'              " 들여쓰기 간격 표시 플러그인 (used)
Plugin 'AutoComplPop'                     " 낱말 자동 완성
" 홈 화면에서 커서 이동을 간편하게 해준다.
" \\w : 뒤 기준으로 이동
" \\W : 앞 기준으로 이동
" \\f : 뒤 기준으로 글자 검색
" \\F : 앞 기준으로 글자 검색
Plugin 'Lokaltog/vim-easymotion'
"Plugin 'blueyed/vim-diminactive'          " 현재 커서가 위치한 부분 강조
"Plugin 'nathanaelkane/vim-indent-guides'  " 들여쓰기 간격 표시 플러그인2

call vundle#end()


" ===== Plugin Setting =====
" -- AutoComplpop 설정
"autocomplpop
function! InsertTabWrapper()
    let col=col('.')-1
    if !col||getline('.')[col-1]!~'\k'
        return "\<TAB>"
    else
        if pumvisible()
            return "\<C-N>"
        else
            return "\<C-N>\<C-P>"
        end
    endif
endfunction

inoremap <TAB> <c-r>=InsertTabWrapper()<cr>

hi Pmenu ctermbg=blue
hi PmenuSel ctermbg=yellow ctermbg=black
hi PmenuSbar ctermbg=blue

" -- ctrlpvim 설정
set wildignore+=*/tmp/*,*.so,*.swp,*.zip
let g:ctrlp_max_files=0  " 모든 파일 탐색
let g:ctrlp_regexp = 1
let g:ctrlp_user_command =
    \ ['.git', 'cd %s && git ls-files -co --exclude-standard']

" -- airline 설정
let g:airline_theme='dracula'

" -- diminactive 설정
let g:diminactive_enable_focus = 1

" -- nerdtree 설정
nmap nerd :NERDTreeToggle<CR>

" --- delimitMate 설정
let delimitMate_expand_cr = 1

" --- (Yggdrrot)indentLine 설정
let g:indentLine_color_term = 239
let g:indentLine_char = '¦'
"let g:indentLine_char_list = ['.', '.', '.', '┊']
"let g:indentLine_conceallevel = 1

" indent guide 설정
"let g:indent_guides_enable_on_vim_startup = 1 " vi 켤때마다 enable  (IndentGuidesEnable로 설정가능)
```
